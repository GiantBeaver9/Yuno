/bin/
/workspaces/
/agents/
*.env
.env
node_modules/
/frontend/node_modules/
# web/dist is committed (built React SPA) so `go build ./cmd/yuno` embeds a
# working UI straight from a clone. Rebuild with `make frontend`.
